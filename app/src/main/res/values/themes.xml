<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RedactorApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primaryDark</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="FullScreen" parent="Theme.RedactorApp">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Base.Theme.MaterialCarousel" parent="Theme.MaterialComponents.DayNight.NoActionBar"/>
    <style name="Theme.MaterialCarousel" parent="Base.Theme.MaterialCarousel"/>
    <style name="Theme.MaterialCarousel.materialComp" parent="Theme.Material3.DayNight.NoActionBar"/>
</resources>